/// Writes a screenshot to a file

fn main() {
    // FIXME: Put this file in the same directory as Cargo.toml
    let filename = "screenshot.webp";

    println!(
        "Imagine this program writing a screenshot into {}",
        filename
    );
}
